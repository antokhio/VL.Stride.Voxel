# Copilot Instructions

## Project Guidelines
- When a Cachable field has an explicit default/initial value in its constructor, the corresponding Set method should also have that default as a parameter default value.